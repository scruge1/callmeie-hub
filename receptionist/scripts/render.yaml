services:
  - type: web
    name: ai-receptionist-server
    runtime: python
    rootDir: scripts
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_FROM_NUMBER
        sync: false
      - key: OWNER_NOTIFICATION_NUMBER
        sync: false
      - key: GOOGLE_SERVICE_ACCOUNT_JSON
        sync: false
      - key: CLIENTS_JSON
        sync: false
      - key: VAPI_API_KEY
        sync: false
      - key: ADMIN_TOKEN
        sync: false
      - key: GOOGLE_SA_EMAIL
        sync: false
      - key: DB_PATH
        sync: false
      - key: CALLMEIE_BACKUP_SHEET_ID
        sync: false
      - key: CALLMEIE_BACKUP_SHEET_TAB
        sync: false
    plan: free
    disk:
      name: callmeie-data
      mountPath: /var/data
      sizeGB: 1
